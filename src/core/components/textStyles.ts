import { StyleSheet } from 'react-native';

export const TextStyles = StyleSheet.create({
  subSectionTitle: {
    fontStyle: 'normal',
    fontWeight: '800',
    fontSize: 14,
    lineHeight: 19,
    color: '#9B9898',
    textTransform: 'uppercase',
  },
});
